# Runtime
NODE_ENV=production
PORT=3001
TRUST_PROXY=1

# CORS (comma-separated). Leave empty to allow same-origin only in production.
ALLOWED_ORIGINS=http://localhost:3001

# Rate limits
RATE_LIMIT_MAX=600
AUTH_RATE_LIMIT_MAX=30

# Database mode
USE_MYSQL=1
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=semo_reptile_house
